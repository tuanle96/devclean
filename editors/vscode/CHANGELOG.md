# Changelog

## 0.1.0

- Show reclaimable workspace bytes in the status bar.
- Scan the active workspace and open a path-explicit HTML report.
- Preview cleanup separately from a modal-confirmed cleanup command.
- Invoke the local `devclean` executable directly without a shell.
